export const environment = {
  production: false,
  apiUrl: 'https://localhost:5001/api',
  // Solo para desarrollo local: permite iniciar sesión con cualquier usuario/contraseña sin llamar al backend.
  loginBypass: true,
  // Solo para desarrollo local: sirve datos de ejemplo en memoria en vez de llamar al backend.
  mockData: true
};
